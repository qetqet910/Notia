version: "1.0"
linter: jetbrains/qodana-js:2025.2
profile:
  name: qodana.recommended

# Install dependencies (improves analysis accuracy)
# Use legacy-peer-deps to handle React 19 conflicts (e.g. react-joyride)
bootstrap: npm install --legacy-peer-deps

# Exclude paths (Rust backend, build artifacts, etc.)
exclude:
  - name: All
    paths:
      - src-tauri
      - dist
      - .vite-cache
      - node_modules
      - .git
      - .idea
      - .devcontainer

# Include specific checks (if needed)
include:
  - name: CheckDependencyLicenses
